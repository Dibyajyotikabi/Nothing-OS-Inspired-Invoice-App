{
  "name": "nothing-invoice-app",
  "version": "1.0.0",
  "description": "Editable invoice app with a print-ready studio invoice design.",
  "type": "module",
  "private": true,
  "scripts": {
    "build:ext": "node scripts/build-ext.mjs",
    "dev": "npx http-server . -p 4173 -c-1",
    "start": "npm run dev"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "playwright": "^1.60.0"
  }
}
